{
  "name": "mongoose-model-decorator",
  "version": "0.0.1",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "dependencies": {
    "tslint": "^6.0.0",
    "typescript": "^3.3.3333"
  },
  "files": [
    "lib/*"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build",
    "release": "standard-version && git push --follow-tags && npm publish"
  },
  "devDependencies": {
    "@types/jest": "25.2.3",
    "@types/mongoose": "5.7.23",
    "jest": "26.0.1",
    "mongoose": "5.9.18",
    "standard-version": "7.1.0",
    "ts-jest": "26.1.0"
  }
}
